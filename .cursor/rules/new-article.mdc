---
description: 新增文章的标准流程
globs: src/content/**/*.md
alwaysApply: false
---

# 新增文章流程

## 步骤

1. 在 `src/content/junior/` 或 `src/content/senior/` 下创建文件夹:
   ```
   src/content/junior/06-prompt-engineering/
   ├── index.md      # 文章内容 (含 frontmatter)
   ├── cover.png      # 封面图
   └── images/        # 文章内引用的图片
   ```

2. `index.md` 顶部添加 frontmatter (参考 content-conventions 规则)

3. 文章头部的系列导航链接使用绝对路径:
   ```markdown
   > 上一篇：[05 | 流式输出](/series/junior/05-streaming) | 下一篇：标题（即将发布）
   ```

4. 上一篇文章的"下一篇预告"链接需要同步更新为新文章的真实路径

5. 系列目录页 (`pages/series/junior/index.astro`) 中"即将更新"列表需更新

## 注意

- 新增文章后无需修改 `content.config.ts`，Astro 自动发现
- `order` 字段确保和文件夹序号一致
- 构建命令验证: `pnpm build`
